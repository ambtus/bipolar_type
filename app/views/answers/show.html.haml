-content_for(:title, "Issue ##{@answer.number} of 4")
%p.center
  Once you have
  %span.chosen chosen
  a Subtype, the other subtypes in the
  %span.warning same
  Realm or Attitude will become unavailable.
-unless @answer.number == 1
  %p.center
    If you get stuck, you can
    =link_to "start over.", answer_path(Answer.first_path)

%table
  -Realm.all.each do |realm|
    %tr
      -realm.subtypes.each do |subtype|
        =render "subtype", subtype: subtype
