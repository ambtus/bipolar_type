- content_for(:title, @subtype.symbol)

%h2=format_from_key(@subtype.symbol).titleize
%p.italic.center Why is it so #{hard(@subtype)}?

%table
  %tr
    %th
    %th Natural Balance
    %th Nurture
    %th Nurtured Imbalance
    %th
    %th New Balance

  %tr
    %th=Help.all.first.name(cookies[:setting])
    %td=format_from_key (@subtype.thing.symbol + 'C')
    %td.left do #{role(@subtype)}
    %td=format_role(@subtype,false)
    %td.left you do not have to #{format_problem(@subtype)} just because you are #{format_role(@subtype).ing}, but it is very hard to do both.
    %td=format_role(@subtype,false)

  %tr
    %th=Help.all.second.name(cookies[:setting])
    %td=format_from_key (@subtype.thing.symbol + 'A')
    %td
    %td=format_from_key (@subtype.thing.symbol + 'A')
    %td.left
    %td=format_from_key (@subtype.thing.symbol + 'A')

  %tr
    %th=Help.all.third.name(cookies[:setting])
    %td=format_from_key (@subtype.thing.symbol + 'R')
    %td
    %td=format_from_key (@subtype.thing.symbol + 'R')
    %td.left
    %td
      =format_from_key (@subtype.thing.symbol + 'R')
      &
      =format_solution(@subtype,false)

  %tr
    %th=Help.all.fourth.name(cookies[:setting])
    %td=format_from_key (@subtype.thing.symbol + 'E')
    %td
    %td=format_from_key (@subtype.thing.symbol + 'E')
    %td.left
    %td=format_from_key (@subtype.thing.symbol + 'E')
