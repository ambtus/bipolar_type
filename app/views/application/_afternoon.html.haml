-case subtype.season
-when "spring", "winter"
  #{subtype.get_extras} if you need extra #{subtype.informal_energy}, otherwise #{subtype.get_staples}
-when "summer", "autumn"
  #{subtype.achieve} new #{subtype.goals} if you need to, otherwise just #{subtype.practice}

