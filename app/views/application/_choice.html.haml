-Realm.each do |realm|
  -choice = realm + phase
  %p{:class => @answer.css(choice)}
    -if @answer.css(choice) == "free"
      =link_to answer_path(@answer.next(choice)) do
        =choice.description.html_safe
    -else
      =choice.name
