%table
  %tr
    %td.bottom{class: phases.first.css}
      .left 🌻
      %b=phases.first.respond.capitalize
      -if balances.blank?
        %div=phases.first.act
      -else
        .left=balances.first
      .right 🌕

    %td

    %td.bottom{class: phases.second.css}
      .right 🍂
      %b=phases.second.respond.capitalize
      -if balances.blank?
        %div=phases.second.act
      -else
        .left=balances.second
      .left 🌗

  %tr
    %td
    %td &orarr;
    %td

  %tr
    %td.top{class: phases.third.css}
      .right 🌓
      %b=phases.third.respond.capitalize
      -if balances.blank?
        %div=phases.third.act
      -else
        .left=balances.third
      .left 🌱

    %td

    %td.top{class: phases.fourth.css}
      .left 🌑
      %b=phases.fourth.respond.capitalize
      -if balances.blank?
        %div=phases.fourth.act
      -else
        .left=balances.fourth
      .right 🌲
