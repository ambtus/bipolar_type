-if subtype.generic?
  You
-else
  Because you are insensitive to
  =subtype.triggers.comma
  -if subtype.consumer?
    if you cannot
    =subtype.produce
    more you
will find it easier to
=subtype.consume_verb
less if you
=subtype.consume
two
==#{(subtype.density * 1.5).to_int}-#{subtype.kinetics}-per-#{subtype.resource}
=subtype.resource.pluralize
or, if you can, one
==#{subtype.density * 3}-#{subtype.kinetics}-per-#{subtype.resource}
=subtype.resource.period
