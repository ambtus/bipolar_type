%table.center
  %caption
    %p.wide Depression is a time when you are much less sensitive to stimulation, which makes you feel like you have much less energy. For this reason, despite consuming more, you produce less.
  -subtypes.in_groups_of(2) do |row|
    %tr
      -row.each do |subtype|
        %td.half=render "depression", :subtype => subtype