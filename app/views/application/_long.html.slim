b=s.episode
br
em=s.flop.goal.too_much
-if @all || s.index > 1
  br
  em=s.flip.goal.too_little
ol.left
  li
    =s.goal
    |<> and whenever you have
    =s.flip.goal.en.too_much

  -if @all || s.index.positive?
    li
      =s.goal
      |<>
      =s.flop.time
      |<> even if you feel
      =s.adverb
      |<>
      =s.flop.bad
      |<> and want to
      =s.flop.goal
      |<> because you have
      =s.opposite.goal.too_much.en


  -if @all || s.index > 1
    li
      =s.flip.goal
      |<>
      =s.opposite.time
      |<> even if you feel
      =s.adverb
      |<>
      =s.opposite.bad
      |<> and want to
      =s.opposite.goal
      |<> because you have
      =s.flop.goal.too_much.en


  -if @all || s.index > 2
    li
      =s.flip.goal
      |<>
      =s.flip.time
      |<> and whenever you feel
      =s.adverb
      |<>
      =s.flip.bad
