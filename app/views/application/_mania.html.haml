-if subtype.generic?
  You
-else
  Because you are sensitive to
  =subtype.triggers.comma
  -unless subtype.consumer?
    if you cannot
    =subtype.produce
    less you
will find it easier to
=subtype.consume_verb
more if you
=subtype.consume
six
==#{subtype.density / 2}-#{subtype.kinetics}-per-#{subtype.resource}
=subtype.resource.pluralize
or, if you can, twelve
==#{subtype.density / 4}-#{subtype.kinetics}-per-#{subtype.resource}
=subtype.resource.pluralize.period
