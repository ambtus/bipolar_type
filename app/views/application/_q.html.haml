I cannot always
=solution(subtype).make_mine('be')
in the
=subtype.normal
=subtype.extreme? ? 'while' : 'when'
I
=solution(subtype.flop).make_mine
but I can
=solution(subtype).make_mine('be')
in the
=subtype.alternate
=subtype.extreme? ? 'when' : 'while'
I
=interference(subtype).make_mine.period
