It’s much easier to
=solution(subtype).make_mine('be')
when I
=succeed '.' do
  -if subtype.extreme?
    don’t have to
    =okay(subtype).make_mine('be')
  -else
    can
    =interference(subtype).make_mine('be')
