%div.bigger In the #{behavior.time}
%ul
  -unless @type.firsts(behavior).empty?
    %li.green.bigger=@type.firsts(behavior).collect{|b| "#{link_to b.eg, b.path, class: 'internal'}"}.join(' & ').html_safe
  -unless @type.seconds(behavior).empty?
    %li.green.big=@type.seconds(behavior).collect{|b| "#{link_to b.eg, b.path, class: 'internal'}"}.join(' & ').html_safe
  %li.yellow=link_to @type.thirds(behavior).eg, @type.thirds(behavior).path, class: "internal"
  %li.red.small=link_to behavior.eg, behavior.path, class: "internal"
