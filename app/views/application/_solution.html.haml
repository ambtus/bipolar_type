-case subtype.nurture
-when "fat"
  #{subtype.get.capitalize} something #{subtype.basic}.
-when "thin"
  #{subtype.get.capitalize} something #{subtype.tempting}.
-when "passive"
  #{subtype.make.capitalize} a new #{subtype.whole} with more #{subtype.part.s}.
-when "active"
  #{subtype.make.capitalize} a new #{subtype.whole} with fewer #{subtype.part.s}.
