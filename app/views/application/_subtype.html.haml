-unless @answer
  -if [Attitude, Realm, Quad].include? @object.class
    %h3=subtype.name
%p.heading
  I
  =subtype.energetic? ? "almost" : "don’t"
  always want to
  =subtype.consume
  =subtype.consume_helper
  =subtype.resources.period
  %br
  I
  =subtype.strong? ? "almost" : "don’t"
  always want to
  =subtype.produce.period
=render(subtype.attitude.domain, :subtype=>subtype) unless @answer
-if [Subtype, Attitude, Realm].include? @object.class
  %p
    =link_to "BipolarTypes starting with this subtype", discover_path(subtype.discover_path)
