%table.center
  -subtypes.each do |subtype|
    %tr
      %td=render "depressed/subtype", :subtype => subtype
      %td=render "subtype", :subtype => subtype
      %td=render "manic/subtype", :subtype => subtype
