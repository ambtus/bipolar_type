-subtypes.in_groups_of(2) do |row|
  %tr
    -row.each do |subtype|
      %td.half
        %h3=link_to subtype.name, discover_path(subtype.discover_path)
        %p=render "subtype", :subtype => subtype
