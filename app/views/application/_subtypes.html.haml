%table
  -@subtypes.in_groups_of(2) do |row|
    %tr
      -row.each do |subtype|
        %td
          =link_to subtype.name, subtype_path(subtype.path)
          %br
          How do I
          =subtype.problem + "?"
          %br
          =subtype.solution.capitalize + "."
