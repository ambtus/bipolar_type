%table
  -@subtypes.in_groups_of(2) do |row|
    %tr
      -row.each do |subtype|
        %td
          %h3=link_to subtype.name, subtype_path(subtype.path)
          %p.center=render "advice", subtype: subtype
