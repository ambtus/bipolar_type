%p
  Being 
  =subtype.domain
  is a balanced state. Over time you will settle into exactly as
  =subtype.potential.many_phrase
  as you need and want. 
%p
  Most of your focus has been on trying to change your nature to
  =subtype.wannabee.name
  by
  =subtype.produce.ing
  more.
%p
  BipolarType asserts that your goal should be to accept your nurture as
  =subtype.goal.name
  by 
  =subtype.consume.ing
  less.
%ul.left
  %li
    if you don’t want to
    =subtype.produce.comma
    don’t
    =subtype.produce 
  %li
    if you are
    =subtype.worn_out.comma
    =subtype.consume
    =subtype.consume_helper
    pure
    =subtype.strengtheners
  %li
    if you feel
    =subtype.empty.comma
    =subtype.consume
    =subtype.consume_helper
    more
    =subtype.buffers