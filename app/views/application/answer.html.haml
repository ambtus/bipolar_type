%p.center
  Once you have
  %span.chosen chosen
  something, other choices will become
  %span.constrained unavailable.
  %br
  If you get stuck you can
  =link_to_unless_current "start over", answer_path(Answer.first_path)

-Problem.each do |p|
  %p.bold=p.issue
  %ul
    -p.subtypes.each do |s|
      %li{:class =>@answer.css(s)}
        =(link_to_if @answer.free?(s), s.things, answer_path(@answer.next(s))) + ':'
        =s.actual
