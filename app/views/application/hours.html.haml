%table
  -Hours.each do |hour|
    %tr
      %th
        =link_to hour.name, type_path(@type.set_hours(hour).symbol)
        .small.unbold=hour.description
      -Phase.each do |phase|
        -if hour.generic?
          %th=hour.range(phase)
        -else
          %td=hour.range(phase)

