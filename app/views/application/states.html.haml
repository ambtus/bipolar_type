- content_for(:title, State.title)

%h2 Four States

%table
  -Mood.each do |mood|
    %tr
      -Aspect.each do |aspect|
        %td.left.bordered
          =render 'cookie', thing: mood + aspect

%p BipolarType asserts that everyone cycles between all four states. Your “type” is the one that is most problematic.
-State.sorted.each do |state|
  %p You self-identify as an #{state.symbol} if you most often need to resolve #{word(state.aspect)} #{word(state.mood)} issues during the #{word(state)} phase. 
