- content_for(:title, @subtype.symbol)

%dl
  %dt Role
  %dd
    to
    =role(@subtype)
  %dt Tendency
  %dd=display @subtype
  %dt Bad Solution
  %dd
    -if @subtype.more?
      =display(transform(@subtype.role, :less))
    -else
      =display(transform(@subtype.herring, :more))
  %dt Actual Problem
  %dd
    -if @subtype.more?
      don’t
      =display(transform(@subtype.solution, :enough))
    -else
      =display(transform(@subtype.problem, :too_many))
  %td Better Solution
  %dd
    -if @subtype.more?
      =display(transform(@subtype.solution, :more))
    -else
      =display(transform(@subtype.problem, :less))

=render 'advice', subtype: @subtype, help: @subtype.help.flip
