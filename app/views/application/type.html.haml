-if cookies.to_h.present?
  .center
    =link_to 'bookmark', type_path(@type.path, cookies.to_h)
    (save your words between sessions)

%br
=render 'cycle', caption: render('chooser'), render: 'advice'

.center
  Legend:
  .after start as soon as possible
  .dont not unless you need to
  .plain whenever you feel like it
  .before continue as long as possible


-if Rails.env.development? && @type == Type.my_type
  %hr
  =link_to 'your type', type_path(Type.your_path)
