-if cookies.to_h.present?
  .center
    =link_to 'bookmark', type_path(@type.path, cookies.to_h)
    |< (save your words between sessions)

br
table
  tr
    -@type.tops.each do |s|
      td.bordered=display(s)
  tr
    -@type.bottoms.each do |s|
      td.bordered=display(s)
br
table
  tr
    -@type.tops.each do |s|
      td.bordered.left=render 'first', s: s
  tr
    -@type.bottoms.each do |s|
      td.bordered.left=render 'first', s: s
br
table.equal
  tr
    -@type.tops.each do |s|
      td.bordered.left=render 'second', s: s, t: @type.other(s)
  tr
    -@type.bottoms.each do |s|
      td.bordered.left=render 'second', s: s, t: @type.other(s)
