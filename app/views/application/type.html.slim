-if cookies.to_h.present?
  .center
    =link_to 'bookmark', type_path(@type.path, cookies.to_h)
    |< (save your words between sessions)

=@type.title
|> :
=render 'type', type: @type

p
  |> I
  =display(@type.problems.first)
  |<> when I am
  =@type.problems.first.sick
  |<> because I am <em>anticipating</em> fear: I feel anxious because I <em>may</em> have to flee the
  =@type.problems.first.focus
  |<> if I cannot flee the
  =@type.problems.second.focus
  |> . I am
  em=@type.problems.second.sick
  |<> if I flee
  =@type.problems.first.foci
  |<> I
  em
    |<> don’t want to flee
  |<> because I have
  em=display(@type.problems.first).too_much.en.bang

p
  |> I
  =display(@type.problems.fourth)
  |<> when I am
  =@type.problems.fourth.sick
  |<> because I am <em>anticipating</em> anger: I feel irritable because I <em>may</em> have to fight the
  =@type.problems.fourth.focus
  |<> if I cannot fight the
  =@type.problems.third.focus
  |> . I am
  em=@type.problems.third.sick
  |<> if I fight
  =@type.problems.fourth.foci
  |<> I
  em
    |<> don’t want to fight
  |<> because I have
  em=display(@type.problems.fourth).too_much.en.bang

-if Rails.env.development?
  hr
  =link_to_unless_current 'my type', type_path(Type.my_path)
  |<>
  =link_to_unless_current 'your type', type_path(Type.your_path)
