-I18n.locale = :en
%table
  -Attitude.all.in_groups_of(2) do |row|
    %tr
      -row.each do |attitude|
        %td.top.quarter
          %h2=link_to attitude.name, attitude_path(attitude.path)
          %p=render attitude.path
