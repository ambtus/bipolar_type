-content_for(:title, "Question ##{@answer.number}")
-Realm.all.each do |realm|
  %p
    I would be thrilled if I woke up and had and had gained a ton of
    =realm.potential.colon
    it would give me an excuse to
    =realm.produce
    as much as I have always wanted to
    =realm.produce.period
    %br
    =link_to_unless @answer.chosen?(realm), "I love to #{realm.produce_things}.", discover_path(@answer.next(realm))

