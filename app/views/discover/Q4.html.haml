- content_for(:title, "Choice ##{@answer.number}")
%p
  Choosing something in a column or row that has already been used will reset one or more of your previous choices.
  The link in the unchosen column and row will take you to the full desciption for one of the 48 BipolarTypes.
  If you don’t like the result, feel free to come back and change your answers.
%table
  %tr
    %th
    -Realm.all.each do |realm|
      %th
        =realm.description
  -Subtype.all.in_groups_of(4).each_with_index do |group, index|
    %tr
      %th
        -attitude=Attitude.all[index]
        =attitude.description
      -group.each do |subtype|
        %td
          -if @answer.free?(subtype)
            %b
              =link_to subtype.short, type_path(@answer.type_path(subtype.letters))
              =surround "(", ")" do
                =subtype.mbti(@answer.basic?(subtype))
          -elsif @answer.chosen?(subtype)
            =subtype.short(@answer.weak?(subtype))
            =surround "(", ")" do
              %b=subtype.mbti(@answer.basic?(subtype))
          -else
            =link_to subtype.short(true), discover_path(@answer.next(subtype.letters))
%p=link_to "Start over", discover_path("Q1")
