-content_for(:title, "#{@answer.realm.name} Tendency (Question ##{@answer.number} of 8)")
%table
  %tr
    -Tendency.all.each do |tendency|
      %td.top.half
        =link_to (render "tendency", tendency: tendency, realm: @answer.realm), discover_path(@answer.next(tendency))
