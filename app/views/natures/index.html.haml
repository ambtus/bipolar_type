- content_for(:title, 'The Four Natures')
%table
  -Nature.all.in_groups_of(2) do |row|
    %tr
      -row.each do |nature|
        %td.top.half
          =render nature.path, subtype: Realm.generic + nature
