Instead of taking stimulants to make the #{realm.objects} seem #{realm.obvious.er}, seek out #{realm.objects} that actually are #{realm.obvious.er}. They don’t need to be #{realm.impactful}, however, as you don’t need the #{realm.kinetic} when you are depressed because you are not #{realm.produce.ing} as much.