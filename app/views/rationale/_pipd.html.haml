If you don’t have any #{realm.nouns} to #{realm.produce_verb} then just #{realm.produce}. You need the practice as you are not very #{realm.adverb} strong. The more you #{realm.produce} now, the easier it will be to #{realm.produce} in the future when you actually have #{realm.nouns} to #{realm.produce_verb}.