%table
  %tr
    %th
    -Realm.all.each do |realm|
      %th=link_to realm.name, realm_path(realm.path)
  -%w{energy strength reserves practice accomplish goals easy hard provide notice collect process resources energizers strengtheners}.each do |word|
    %tr
      %th=word
      -Realm.all.each do |realm|
        %td=realm.send(word)
