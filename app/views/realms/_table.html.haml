%table
  %caption The Four Realms
  %tr
    %th
    -Realm.all.each do |realm|
      %th=link_to realm.name, realm_path(realm.path)
  -%w{ consume resources energy produce }.each do |word|
    %tr
      %th=word.capitalize
      -Realm.all.each do |realm|
        %td=realm.send(word)
