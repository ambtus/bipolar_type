- content_for(:title, "The Four Energies")
%table
  %tr
    %th
    -Realm.all.each do |realm|
      %th=link_to realm.name, realm_path(realm.path)
  -%w{ get use measure }.each do |word|
    %tr
      %th=word.capitalize
      -Realm.all.each do |realm|
        %td=realm.send(word)
