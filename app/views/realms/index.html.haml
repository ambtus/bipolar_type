%table
  %tr
    %th
    -Realm.each do |realm|
      %th=link_to realm.inspect, realm_path(realm.path)
  -%w{energy get receive use produce}.each do |word|
    %tr
      %th=word.titleize
      -Realm.each do |realm|
        %td=realm.send(word)
