- content_for(:title, 'The 4 Attitudes')
%table
  -Attitude.all.in_groups_of(2) do |row|
    %tr
      -row.each do |attitude|
        %td.half.bordered
          =render "square", :subtypes => attitude.subtypes, :caption => attitude
=render "footnote"
