- content_for(:title, 'Depressed behaviors')
%table.center
  -Subtype.all.in_groups_of(4).each do |row|
    %tr
      -row.each do |subtype|
        %td=render "depressed/subtype", :subtype => subtype
