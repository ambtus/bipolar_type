- content_for(:title, @quad.name)
%table
  -@quad.subtypes.in_groups_of(2) do |row|
    %tr
      -row.each do |subtype|
        %td.top.half
          =render "subtype", subtype: subtype
          =render subtype.ordinal, subtype: subtype, paired: @quad.paired(subtype)