- content_for(:title, 'The Four Realms')
%table
  -Realm.all.each do |realm|
    %tr
      %th.narrow=link_to realm.name.suffix(" Subtypes"), type_path(realm.path)
      %td.quarter
        %ul.left
          %li=realm.empty.prefix("feel ")
          %li=realm.consume_triggers.prefix("don’t ")
          %li=realm.potential.prefix("lose ")
      %td.quarter
        %ul.left
          %li=realm.full.prefix("feel ")
          %li=realm.consume_triggers.more_phrase
          %li=realm.potential.prefix("gain ")
      %td.quarter
        %ul.left
          %li=realm.ready.prefix("feel ")
          %li=realm.produce_strongly.prefix("don’t ")
          %li=realm.strengths_atrophy
      %td.quarter
        %ul.left
          %li=realm.worn_out.prefix("feel ")
          %li=realm.consume_strengtheners.more_phrase
          %li=realm.strengths.prefix("gain ")
