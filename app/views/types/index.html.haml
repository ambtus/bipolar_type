- content_for(:title, "The 24 BipolarTypes")
%table
  -Type.all.in_groups_of(2) do |row|
    %tr
      -row.each do |type|
        %td.top
          %h3=link_to type.subtypes.map(&:name).and, type_path(type.path)
          =render "schedule", type: type
